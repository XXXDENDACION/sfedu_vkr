import React from "react";
import styles from "./style.module.css";

const SideBar = () => {
  return (
    <div className={styles.sidebar}>
      <div className={styles.wrapper}>Главное меню</div>
    </div>
  );
};

export default SideBar;
